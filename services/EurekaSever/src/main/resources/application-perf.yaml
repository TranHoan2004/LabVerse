spring:
  application:
    name: EurekaServer
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9001/eureka/, http://localhost:9000/eureka/
  instance:
    hostname: labverse
server:
  port: 9002