spring:
  application:
    name: EurekaServer
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      #      defaultZone: http://localhost:9001/eureka/, http://library:9002/eureka
      defaultZone: http://localhost:9000/eureka/
server:
  port: 9001
