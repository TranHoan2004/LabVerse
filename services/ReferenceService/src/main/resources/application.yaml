spring:
  application:
    name: se1853_jv
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
server:
  servlet:
    context-path: /v1/api
  port: 8081
